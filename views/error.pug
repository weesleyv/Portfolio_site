extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
            
            h1 message: #{error.message}
            h2 status: #{error.status}
            p stack: #{error.stack}

    